# Core crawling, extraction, and normalization code uses Python standard library only.
# Keep lightweight runtime dependencies shared with quality CI.
-r requirements-core.txt
#
# OCR is required for image-based notices, such as Hebei's price table images.
paddlepaddle==3.2.0
paddleocr==3.3.3
xlrd==2.0.1
pypdfium2==4.30.1
cloakbrowser==0.5.7
